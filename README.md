# Products Microservice

## Dev

1. Clone repository
2. Install dependencies `pnpm install`
3. Create a `.env` file with `.env.template`
4. Run `npx prisma migrate deploy`
5. Run `npx prisma generate`
6. Run `pnpm start:dev`
